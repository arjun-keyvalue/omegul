propel:
    database:
        connections:
            default:
                adapter: pgsql
                dsn: pgsql:host=localhost;port=5432;dbname=postgres
                user: postgres
                password: password
                settings:
                    charset: utf8
